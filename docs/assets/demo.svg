<svg xmlns="http://www.w3.org/2000/svg" width="1060" height="600" viewBox="0 0 1060 600" font-family="Menlo, Consolas, 'DejaVu Sans Mono', monospace" font-size="13">
  <!-- Terminal transcript of a real run against examples/DECISIONS.md with
       EMBERLOG_TODAY=2026-08-01. Text is copied from an actual execution;
       long lint messages are trimmed with an ellipsis to fit the frame. -->
  <rect width="1060" height="600" rx="10" fill="#0d1117"/>
  <rect width="1060" height="34" rx="10" fill="#161b22"/>
  <rect y="20" width="1060" height="14" fill="#161b22"/>
  <circle cx="22" cy="17" r="6" fill="#ff5f57"/>
  <circle cx="42" cy="17" r="6" fill="#febc2e"/>
  <circle cx="62" cy="17" r="6" fill="#28c840"/>
  <text x="530" y="22" fill="#8b949e" text-anchor="middle" font-size="12">emberlog — decision logs that know when they rot</text>
  <g fill="#c9d1d9">
    <text x="24" y="64"><tspan fill="#7ee787">$</tspan> emberlog lint -f DECISIONS.md</text>
    <text x="24" y="92">DECISIONS.md:20: <tspan fill="#f85149">E101</tspan> expired: "Staging resets its database every Monday" expired 2026-07-16 (16d ago)…</text>
    <text x="24" y="116">DECISIONS.md:25: <tspan fill="#f85149">E101</tspan> expired: "The flaky checkout test is probably the shared cart fixture" expired…</text>
    <text x="24" y="140">DECISIONS.md:25: <tspan fill="#d29922">W205</tspan> stale-unverified: "The flaky checkout test…" is still confidence=guess after 52d…</text>
    <text x="24" y="164">DECISIONS.md:30: <tspan fill="#d29922">W203</tspan> no-provenance: "Payment provider sandbox rate limit is 60 rpm" has no source=…</text>
    <text x="24" y="188">DECISIONS.md: 4 findings (<tspan fill="#f85149">2 errors</tspan>, <tspan fill="#d29922">2 warnings</tspan>)</text>
    <text x="24" y="216" fill="#8b949e"># every one of those entries would have been read as truth next session</text>
    <text x="24" y="256"><tspan fill="#7ee787">$</tspan> emberlog sweep -f DECISIONS.md</text>
    <text x="24" y="284">  3430c6 "Staging resets its database every Monday" (expired 2026-07-16, 16d ago)</text>
    <text x="24" y="308">  812d9a "The flaky checkout test is probably the shared cart fixture" (expired 2026-07-10, 22d ago)</text>
    <text x="24" y="332" fill="#7ee787">swept 2 expired entries -&gt; DECISIONS.archive.md</text>
    <text x="24" y="372"><tspan fill="#7ee787">$</tspan> emberlog list -f DECISIONS.md</text>
    <text x="24" y="400" fill="#8b949e">ID      AGE  EXPIRES  CONF      SOURCE             TITLE</text>
    <text x="24" y="424">6e28c8  19d  <tspan fill="#7ee787">in 71d</tspan>   observed  agent:claude-code  Use SQLite for the job queue</text>
    <text x="24" y="448">b2ac04  19d  <tspan fill="#7ee787">never</tspan>    verified  human:alice        Deploys happen from main only</text>
    <text x="24" y="472">e91125  73d  <tspan fill="#7ee787">in 111d</tspan>  <tspan fill="#d29922">-</tspan>         <tspan fill="#d29922">-</tspan>                  Payment provider sandbox rate limit is 60 rpm</text>
    <text x="24" y="512"><tspan fill="#7ee787">$</tspan> emberlog lint -f DECISIONS.md; echo exit=$?</text>
    <text x="24" y="540">DECISIONS.md:20: <tspan fill="#d29922">W203</tspan> no-provenance: "Payment provider sandbox rate limit is 60 rpm" has no source=…</text>
    <text x="24" y="564">DECISIONS.md: 1 finding (0 errors, <tspan fill="#d29922">1 warning</tspan>)   <tspan fill="#7ee787">exit=0</tspan>   <tspan fill="#8b949e"># errors gone; --strict would gate on the warning too</tspan></text>
  </g>
</svg>
